# PyTorch (如果云平台未预装，取消注释)
# torch>=2.0.0

# Transformers 库 (GPT-2 tokenizer)
transformers>=4.30.0

# SentencePiece (自定义 tokenizer)
sentencepiece>=0.1.99

# TensorBoard (训练监控)
tensorboard>=2.13.0

# 进度条
tqdm>=4.65.0

# 评估指标
sacrebleu>=2.3.0

# 数据处理 (可选)
datasets>=2.10.0

# 可视化 (可选)
matplotlib>=3.7.0
